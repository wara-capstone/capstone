server {
    listen 80;
    server_name www.onoff.zone;  # 여기에 도메인 이름을 입력하세요.

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}
